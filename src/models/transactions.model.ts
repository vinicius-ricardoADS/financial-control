export enum RecurrenceTypes {
  FIXED = 'fixa',
  VARIABLE = 'variavel',
  RECURRENCE = 'recorrente',
  LOOSE = 'avulsa',
}

export enum RecurrenceTypesId {
  FIXED = 1,
  VARIABLE = 2,
  RECURRENCE = 3,
  LOOSE = 4,
}